# Monitoring API ishlatilmaydi (tizimga kerak emas) — ochiq port qoldirmaslik uchun o'chirilgan.
# Konteyner sog'ligi docker-compose healthcheck orqali (UDP 2055 tinglanyaptimi) tekshiriladi.
api.enabled: false
xpack.monitoring.enabled: false
pipeline.ecs_compatibility: disabled
pipeline.batch.size: 1000
pipeline.workers: ${NSEL_WORKERS:4}
